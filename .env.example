# Copy to .env in the project root (next to package.json).
# Use the "ApiUrl" output of the deployed stack. Rebuild the web app after changing it.
EXPO_PUBLIC_API_URL=<amazon_public_api>
# Uncomment to force the original offline demo (mock data) even when the URL is set:
# EXPO_PUBLIC_USE_MOCK=true
